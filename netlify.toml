# Netlify Configuration for VishwaGuru Frontend

# Build settings
[build]
  base = "frontend"
  publish = "dist"
  command = "npm install && npm run build"

# Build environment
[build.environment]
  NODE_VERSION = "20"
