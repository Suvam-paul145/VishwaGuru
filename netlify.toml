# Netlify Configuration for VishwaGuru Frontend

# Build settings
[build]
  base = "frontend"
  publish = "dist"
  command = "npm install && npm run build"

# Build environment
[build.environment]
  NODE_VERSION = "20"

# Note: Redirects and Headers are now handled by _redirects and _headers files in frontend/public/
